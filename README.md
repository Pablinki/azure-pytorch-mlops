# azure-pytorch-mlops

PyTorch text classifier (`textclf`, AG News, TextCNN) trained on Azure Machine Learning and served as an
autoscaling FastAPI service on Azure Container Apps. Infrastructure in Bicep, delivery with GitHub Actions + OIDC.

> Work in progress: this README is completed in Phase 10 of the build plan.
